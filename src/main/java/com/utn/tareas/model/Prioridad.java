package com.utn.tareas.model;

/**
 * Enum que representa los niveles de prioridad de una tarea.
 * Se usa enum porque son valores constantes y predefinidos.
 */
public enum Prioridad {
    ALTA,
    MEDIA,
    BAJA
}